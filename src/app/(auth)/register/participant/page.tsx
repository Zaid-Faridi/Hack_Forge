import { DualLoginPortal } from '@/components/auth/DualLoginPortal';

export const metadata = { title: 'Sign Up — Participant' };

export default function ParticipantRegisterPage() {
    return <DualLoginPortal />;
}
